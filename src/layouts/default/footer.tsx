import styled from "@emotion/styled";

export function Footer() {
  return (
    <>
      <StyledFooter>
        <div></div>
      </StyledFooter>
    </>
  );
}

const StyledFooter = styled("footer")({});
